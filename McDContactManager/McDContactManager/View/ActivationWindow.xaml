<Window x:Class="McDContactManager.View.ActivationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:McDContactManager.ViewModel"
        xmlns:local="clr-namespace:McDContactManager.View"
        mc:Ignorable="d"
        Title="Aktiválás" Height="200" Width="400">
    
    <Window.DataContext>
        <vm:ActivationWindowViewModel />
    </Window.DataContext>
    
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Írd be a kliens id-t és kliens secret-et:" Margin="0,0,0,10"/>
            <TextBox Text="{Binding ActivationId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <TextBox Text="{Binding ActivationSecret, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <Button Content="Aktiválás" Command="{Binding ActivationCommand}"/>
        </StackPanel>
    </Grid>
</Window>
